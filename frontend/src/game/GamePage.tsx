import { Box } from '@chakra-ui/react';
import PixelGrid from "./GamePage/PixelGrid.tsx";

export const GamePage = () => {
    return (
        <Box>
            <PixelGrid />
        </Box>
    );
};
export default GamePage;